import type { NextPage } from "next";

const Error: NextPage = () => {
  return (
    <div className="flex-x-center h-screen w-screen text-xs italic text-gray-500">
      Inappropriate behavior has been detected.
    </div>
  );
};

export default Error;
